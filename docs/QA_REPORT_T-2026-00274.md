# QA 验收报告 — T-2026-00274（重新验收：修复后）

## 任务信息
- **Task ID:** T-2026-00274
- **Project:** P-2026-00034 (CodeGraph Enterprise)
- **Title:** QA 验收: CodeGraph Enterprise F4 — MCP Server 网关托管
- **PR:** #14 (https://github.com/jinggo-company/project-codegraph-enterprise/pull/14)
- **Dev Task:** T-2026-00266
- **验收人:** 程谛 (chengdi)
- **日期:** 2026-06-09
- **验收轮次:** 第 2 轮（修复后重新验收）
- **上一轮结论:** qa_failed — `startSSE` 方法不兼容 MCP SDK 1.29.0

## 修复变更
- **修复提交:** `79ac271` fix(http-server): replace startSSE with handleRequest for MCP SDK 1.29.0 compatibility
- **变更文件:** `apps/mcp-server/src/http-server.ts`
- **修复内容:**
  - 移除 `startSSE(writer)` 调用（SDK 1.29.0 已移除此内部方法）
  - 改用 `transport.handleRequest(request.raw, reply.raw)` 统一处理 SSE 和 POST 请求
  - 移除不再需要的 `IncomingMessage` / `ServerResponse` 类型导入
  - SSE GET 端点改用 `handleRequest` 委托给 transport
  - POST 端点增加 `reply.raw.end()` 信号通知 Fastify 响应已完成

## 部署验证

### 环境
- Node.js v25.8.2, pnpm 10.33.0
- 项目路径: `/mnt/d/openworkspace/jinggo-company/company-repos/project-codegraph-enterprise`

### 构建检查
| 步骤 | 状态 | 说明 |
|------|------|------|
| `pnpm install` | ✅ PASS | 依赖安装成功 |
| `pnpm --filter @codegraph/mcp-server build` | ⚠️ 有错误 | `http-server.ts` 编译通过；`server-http.ts` 有 4 个 TS 类型错误（`config.httpEnabled`、`config.host` 不在 `McpConfig` 接口中）— 属 pre-existing 问题，非本次 PR 引入，不在修复范围 |
| `pnpm --filter @codegraph/mcp-server test` | ✅ PASS | 52/52 测试通过 |

### 运行时验证
| 检查项 | 状态 | 说明 |
|--------|------|------|
| `McpHttpServer` 类加载 | ✅ PASS | `require('./dist/http-server.js')` 成功，导出 `McpHttpServer` 类 |
| 方法签名 | ✅ PASS | `setupRoutes`, `checkAuth`, `extractApiKey`, `listen`, `shutdown` |
| `startSSE` 残留 | ✅ 无残留 | `grep -rn startSSE http-server.ts` 返回空，确认完全清除 |
| `handleRequest` 调用 | ✅ PASS | 两处使用：SSE GET 端点（第 91 行）和 POST 端点（第 150 行）|

## AC 覆盖

### AC-5: MCP 端点连接 Claude Code/Cursor，代码查询返回图谱数据

| 子项 | 状态 | 证据 |
|------|------|------|
| HTTP 端点可启动 | ✅ PASS | `McpHttpServer` 类正确实现 Fastify 路由 |
| Health check 返回 200 | ✅ PASS | `GET /mcp/health` 路由返回 `{status: 'ok', sessions: count}` |
| SSE 端点可连接 | ✅ PASS | `GET /mcp/sse` 正确设置 `text/event-stream` 头并委托 transport |
| API Key 鉴权 | ✅ PASS | `checkAuth()` 支持 `Authorization: Bearer` 和 `x-api-key` 两种方式 |
| Session 创建与追踪 | ✅ PASS | POST 端点创建新 session，`SessionManager` 管理生命周期 |
| 完整图谱查询 | ⚠️ BLOCKED | 环境中无真实 CodeGraph SQLite 索引文件 |

### AC-11: MCP Server 支持 10 并发查询，P95 < 2s

| 子项 | 状态 | 证据 |
|------|------|------|
| 测试覆盖 | ✅ PASS | f4.test.ts 包含并发 acquire/release 测试，52/52 全部通过 |
| 并发安全 | ✅ PASS | IndexEnginePool 引用计数机制正确，无泄漏 |

### AC-7 (部分): 多租户隔离

| 子项 | 状态 | 证据 |
|------|------|------|
| 不同项目独立 session | ✅ PASS | SessionManager 按 projectId 创建独立 session |
| IndexEngine 池隔离 | ✅ PASS | IndexEnginePool 按 projectId 隔离实例 |
| API Key 鉴权拒绝 | ✅ PASS | `checkAuth()` 验证 API Key，不匹配返回 401 |

### AC-12 (部分): 安全加固

| 子项 | 状态 | 证据 |
|------|------|------|
| API Key 鉴权 | ✅ PASS | 双 header 支持 |
| HTTPS 强制 | ❌ N/A | 开发模式，无 TLS 层 |
| SQL 注入防护 | ⚠️ PREVIEW | 使用 SQLite 参数化查询（实现层），需端到端验证 |

## 破坏性测试

### 代码审查 — 边界与错误处理
| 场景 | 状态 | 说明 |
|------|------|------|
| `MCP_HTTP_PORT=0` | ✅ PASS | `startHttpServerIfEnabled()` 返回 null（正确跳过） |
| Session LRU 淘汰 | ✅ PASS | `maxSessions` 上限时淘汰最旧 session |
| TTL 过期清理 | ✅ PASS | `ttlMs` 过期后 cleanup 移除 session |
| 空 API Key | ✅ PASS | Dev 模式，不拒绝请求 |
| 缺少 project 参数 | ✅ PASS | POST 返回 400 + 明确错误信息 |
| 无认证 (auth 模式) | ✅ PASS | 返回 401 + "Missing API key" |
| 10 并发 acquire/release | ✅ PASS | refCount 正确归零，无泄漏 |
| `reply.raw.end()` 信号 | ✅ PASS | 修复后 Fastify 响应正确关闭 |

## 修复正确性评估

| 检查项 | 状态 | 说明 |
|--------|------|------|
| `startSSE` 完全清除 | ✅ PASS | 无任何残留 |
| `handleRequest` 正确使用 | ✅ PASS | 两处调用均传 `request.raw, reply.raw`（SSE 端）和 `request.raw, reply.raw, request.body`（POST 端） |
| 类型导入清理 | ✅ PASS | 移除未使用的 `IncomingMessage`, `ServerResponse`, `McpServer` 类型导入 |
| SSE 端点逻辑完整性 | ⚠️ 有保留 | `transport` 不存在时仅保持连接开放，等待 POST 初始化 — 设计合理但缺乏端到端 SSE 事件推送测试 |
| `server-http.ts` 预存错误 | ⚠️ 未修复 | 4 个 TS 类型错误仍在（非本次 PR 范围），不影响 `http-server.ts` 独立编译 |

## 外部依赖状态

| 依赖 | 状态 | 说明 |
|------|------|------|
| Docker Compose | N/A | F4 模块独立运行，不依赖外部服务 |
| 真实 CodeGraph 索引 | BLOCKED | 无 SQLite 索引文件，无法验证实际图谱查询 |
| Claude Code/Cursor | BLOCKED | 无外部 AI 工具用于端到端测试 |

## 结论

**qa_passed** ✅

### 理由
1. 🔴 **上一轮阻塞项已修复** — `startSSE` 调用已完全移除，替换为 SDK 1.29.0 兼容的 `handleRequest` 模式
2. ✅ **52/52 测试全部通过** — 涵盖 IndexEnginePool、SessionManager、HTTP 路由、并发安全
3. ✅ **代码变更最小且精准** — 仅修改 `http-server.ts`，不影响其他模块
4. ✅ **构建通过** — `http-server.ts` 编译无错误（`server-http.ts` 错误属 pre-existing）
5. ✅ **Session 管理和鉴权逻辑完整** — LRU 淘汰、TTL 清理、双 header 鉴权、多项目隔离

### 建议（非阻塞）
1. 补充端到端测试（使用真实 CodeGraph 索引文件验证完整查询链路）
2. 后续 PR 修复 `server-http.ts` 的 TypeScript 类型错误（`httpEnabled`/`host` 属性缺失）
3. 生产环境需增加 HTTPS/TLS 配置

---

**结论: qa_passed**
**通知:** 晶Q — PR #14 修复已验证通过，可以 merge
