{
  "name": "@codegraph/db",
  "version": "0.1.0",
  "private": true,
  "description": "Prisma database client and schema",
  "main": "./src/client.ts",
  "types": "./src/client.ts",
  "scripts": {
    "build": "tsc",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.15.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prisma": "^5.15.0",
    "typescript": "^5.5.0"
  }
}
